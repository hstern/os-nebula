# os-nebula

OPNsense plugin for the [Nebula](https://github.com/slackhq/nebula) mesh overlay VPN.
Tag `vX.Y` to build and publish `os-nebula-X.Y.pkg` via GitHub Actions; the package
is served from the [stern OPNsense repo](https://github.com/hstern/opnsense-repo).
